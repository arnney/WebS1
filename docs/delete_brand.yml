parameters:
  - name: brand_id
    in: path
    type: string
    required: true
    description: The ID of the brand to delete
responses:
  204:
    description:
  404:
    description: Brand not found